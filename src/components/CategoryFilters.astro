---
import { listCategories, type CategoryId } from '../lib/categories';

type Props = {
  active?: CategoryId | 'all';
};

const { active = 'all' } = Astro.props;
const categories = listCategories();
---

<nav class="category-filters" aria-label="Blog categories">
  <a href="/blog/" aria-current={active === 'all' ? 'true' : undefined}>All</a>
  {
    categories.map((category) => (
      <a href={category.path} aria-current={active === category.id ? 'true' : undefined}>
        {category.name}
      </a>
    ))
  }
</nav>
